on:
  push:
  workflow_dispatch:

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
#     - name: Checkout
#       uses: actions/checkout@v2
#     - name: Hello world action step
#       id: hello
#       uses: ./
#       with:
#         who-to-greet: 'Mudit The Developer'
#     # Use the output from the `hello` step
#     - name: Get the output time
#       run: echo "The time was ${{ steps.hello.outputs.time }}"
    - name: Gen commands
      run: |
        var=$(curl -X GET "https://api.todoist.com/rest/v1/projects/2264420260" -H "Authorization: Bearer 7bfca71a9f3343895e02d7d8c70979c2003fb3ce" | jq '.name')
        echo $var
